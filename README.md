# solana-sdp

Ruby client for the [Solana Developer Platform](https://github.com/solana-foundation/solana-developer-platform) (SDP) wallets and payments API. Plain Ruby, zero runtime dependencies (Net::HTTP), typed rescuable errors, and a retry posture that never re-sends a transfer. Tested against SDP v0.28.
